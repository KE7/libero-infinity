"""Premerge validation harness for libero-infinity (G0–G7).

See ``sweep.py`` for the condition-coverage driver.

G7 human-review submodules:
- :mod:`review_packet` — generate a per-task review packet.
- :mod:`review_sampler` — three-layer sampling driver.
- :mod:`review_aggregate` — aggregate filled checklists into severity / κ / α stats.
"""
